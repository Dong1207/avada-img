{
  "name": "frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:functions": "npm run build && wrangler pages dev dist --port 3000",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=avada-image"
  },
  "devDependencies": {
    "vite": "7.2.2",
    "terser": "^5.26.0",
    "wrangler": "4.47.0"
  },
  "engines": {
    "node": ">=20.19.0 || >=22.12.0"
  }
}

